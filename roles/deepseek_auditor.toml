id = "deepseek_auditor"
description = "Independent read-only audit of worker results and diffs."
sandbox = "read-only"
sandbox_network_access = false
instructions = "Audit independently; never repair findings and never delegate."
